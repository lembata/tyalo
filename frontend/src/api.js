export const API = {
  IsLoggedIn: () => {
    //return localStorage.getItem('token') !== null;
    //TODO: Send request to server to check if token is valid
    return true
  }
}

export default API

